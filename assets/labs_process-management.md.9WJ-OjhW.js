import{_ as s,o as t,c as a,a0 as i}from"./chunks/framework.1R7PBCUH.js";const u=JSON.parse('{"title":"Process Management","description":"","frontmatter":{},"headers":[],"relativePath":"labs/process-management.md","filePath":"labs/process-management.md"}'),o={name:"labs/process-management.md"};function n(r,e,h,p,l,c){return t(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="process-management" tabindex="-1">Process Management <a class="header-anchor" href="#process-management" aria-label="Permalink to &quot;Process Management&quot;">​</a></h1><p>In this lab, you’ll learn how an operating system runs applications.</p><p>You’ll learn about <strong>processes</strong>, their life cycle, and how you can monitor their activity. Understanding how processes work and how to manage them is crucial for system administration, performance optimization, and troubleshooting.</p><h2 id="processes" tabindex="-1">Processes <a class="header-anchor" href="#processes" aria-label="Permalink to &quot;Processes&quot;">​</a></h2><p>When you compile the source code for an application, the compiler outputs an executable file that contains instructions for the CPU. When you run this file, the operating system creates a <strong>process</strong>. This process represents a running instance of the application and is responsible for executing its code and managing its resources. You can run the same application multiple times; each run creates a new process.</p><p>The operating system tracks the state of every process in a <strong>process table</strong> and provides processes with the resources they need to run their code, most notably <strong>memory</strong> and <strong>CPU time</strong>.</p><h3 id="address-space" tabindex="-1">Address space <a class="header-anchor" href="#address-space" aria-label="Permalink to &quot;Address space&quot;">​</a></h3><p>The operating system reserves a piece of memory for every process it creates. This memory is known as the <strong>address space</strong> of the process and is divided into segments:</p><ul><li>The <strong>text</strong> segment contains instructions for the CPU, loaded from the executable file. This segment is read-only, so processes running the same executable can share a text segment.</li><li>The <strong>data</strong> segment contains global and static variables used by the executable.</li><li>The <strong>stack</strong> segment is used to implement function calls and is managed by the CPU. Every time the CPU encounters a function call, it reserves enough memory on the stack to store that function’s parameters, local variables, and return address. When the function ends, its memory is removed from the stack.</li><li>The <strong>heap</strong> segment stores data whose lifetime is not bound to a single function and data that is too large to be put on the stack. Some programming languages require the programmer to manually allocate and free heap memory; other languages rely on reference counting or garbage collection to manage the heap for you.</li></ul><p>The organization of the address space may differ between operating systems and hardware architectures, but these segments are common across most systems.</p><h3 id="virtual-memory" tabindex="-1">Virtual memory <a class="header-anchor" href="#virtual-memory" aria-label="Permalink to &quot;Virtual memory&quot;">​</a></h3><p>Operating systems rely on <strong>virtual memory</strong> to improve their security and stability. With virtual memory, processes are assigned a virtual address space whose addresses do not directly correspond with physical memory locations. This virtual address space gives each process the illusion of having exclusive access to the entire memory, while in reality, it can only access a small part of it.</p><p>The operating system works together with the <strong>memory management unit (MMU)</strong> of the CPU to translate virtual addresses into physical addresses. This layer of indirection ensures that processes cannot access memory assigned to other processes.</p><h3 id="scheduling" tabindex="-1">Scheduling <a class="header-anchor" href="#scheduling" aria-label="Permalink to &quot;Scheduling&quot;">​</a></h3><p>Processes need access to the CPU to execute their instructions. However, a CPU only has a small number of execution cores, limiting the number of processes it can execute in parallel. When the number of processes exceeds the number of execution cores, processes take turns sharing the CPU.</p><p>Operating systems use a <strong>scheduler</strong> to allocate CPU time to processes. Schedulers can handle a large number of processes and use various algorithms to balance fairness with overall system performance.</p><p>A scheduler starts by checking the state of each process:</p><ul><li>A process is in the <strong>running</strong> state when it’s able to continue executing. A running process is either executing on the CPU or waiting in the run queue. In both cases, the process is eligible to be scheduled for CPU time.</li><li>A process is in the <strong>sleeping</strong> state when it’s waiting for an event. It may be waiting for input from the user, a timer to run out, or data to become available. A sleeping process is blocked and is unable to continue executing until the event it’s waiting for occurs. At that time, the process will rejoin the run queue.</li></ul><p>The scheduler considers all running processes, determines the next process to execute, and allocates it CPU time.</p><p>When the time comes to execute the next process, the operating system performs a <strong>context switch</strong>:</p><ol><li>The currently executing process is paused.</li><li>The operating system stores the current state of the CPU in the process table, so the paused process can be resumed later.</li><li>The operating system loads the state of the next process from the process table and configures the CPU.</li><li>The CPU resumes executing.</li></ol><p>Rapidly switching between processes creates the illusion that all processes are executing simultaneously. However, context switches are expensive operations for the CPU and they can reduce system performance. Schedulers must balance these factors and optimize the number of context switches.</p><p>Schedulers must also prevent any single process from monopolizing the CPU. Most schedulers are <strong>preemptive</strong>: they can pause a running process when it has used up its allocated CPU time. A non-preemptive scheduler must wait until a process finishes executing, goes to sleep, or voluntarily yields the CPU.</p><p>An example of preemptive scheduling is <strong>round robin</strong>. This algorithm allocates each process a fixed time slice (e.g. 100ms) and preempts a process when its time is up. Processes that finish their turn move to the back of the queue and wait until all other processes are given a turn before resuming their work.</p><p>This algorithm seems fair but performs poorly in most real-world scenarios. Linux implements a far more advanced scheduling algorithm that uses variable time slices and takes into account the weight or priority of a process, the amount of CPU time it has used so far, and whether or not the process is interactive and requires quick response times.</p><h2 id="process-overview" tabindex="-1">Process overview <a class="header-anchor" href="#process-overview" aria-label="Permalink to &quot;Process overview&quot;">​</a></h2><p>A Linux distribution includes many useful commands to look up processes and monitor their activity.</p><p>The <strong><code>ps</code></strong> (<em>process status</em>) command lists active processes:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ps</span></span></code></pre></div><p>The output of <code>ps</code> contains the following columns:</p><table tabindex="0"><thead><tr><th>Column</th><th>Description</th></tr></thead><tbody><tr><td>PID</td><td>The <strong>Process ID</strong>, a unique identifier for the process.</td></tr><tr><td>TTY</td><td>The terminal (“teletype”) associated with the process.<br>Background processes show a question mark (<code>?</code>) in this column.</td></tr><tr><td>TIME</td><td>The total CPU time consumed by the process since it started.</td></tr><tr><td>CMD</td><td>The executable or command this process is running.</td></tr></tbody></table><p>By default, <code>ps</code> only shows processes in your current session. Add the <code>-e</code> option to see every process on the system:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ps</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -e</span></span></code></pre></div><p>You may be surprised to see how many processes are active on your system.</p><p>The <strong><code>top</code></strong> command displays process activity in real-time:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">top</span></span></code></pre></div><p><code>top</code> shows a lot of information. For now, focus on the following columns:</p><table tabindex="0"><thead><tr><th>Column</th><th>Description</th></tr></thead><tbody><tr><td>PID</td><td>The Process ID.</td></tr><tr><td>USER</td><td>The user that owns this process.</td></tr><tr><td>RES</td><td>The amount of memory being used by the process (“resident memory”).<br>Press <code>e</code> to display this amount in a different unit.</td></tr><tr><td>S</td><td>The current state of the process.<br><code>R</code> indicates a running process.<br><code>S</code> indicates a sleeping process.</td></tr><tr><td>%CPU</td><td>The percentage of CPU time being used by the process.<br>This can exceed 100% if the process uses multiple CPU cores.<br>Press <code>P</code> to sort by this amount.</td></tr><tr><td>%MEM</td><td>The percentage of memory being used by the process.<br>Press <code>M</code> to sort by this amount.</td></tr><tr><td>TIME+</td><td>The total CPU time consumed by the process since it started.<br>Press <code>T</code> to sort by this amount.</td></tr><tr><td>CMD</td><td>The executable or command this process is running.</td></tr></tbody></table><p>When you’re done, press <code>q</code> to quit <code>top</code>.</p><div class="info custom-block"><p class="custom-block-title">Note</p><p><code>top</code> is not very user-friendly. Most users prefer an alternative command, such as <strong><code>htop</code></strong>, which provides a more intuitive interface, color coding, and additional features. You’ll install and use <code>htop</code> in <a href="./../exercises/exercises5.html#htop">Exercise 5.3</a>.</p></div><h2 id="starting-processes" tabindex="-1">Starting processes <a class="header-anchor" href="#starting-processes" aria-label="Permalink to &quot;Starting processes&quot;">​</a></h2><p>Every time you run a command in the terminal or start an application through the graphical user interface, the operating system creates a new process and assigns it a unique PID.</p><p>For example, start Firefox by clicking its icon in the dock, then run the following command:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ps</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -e</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> firefox</span></span></code></pre></div><p>This command uses <code>grep</code> to find the newly created process in the output of <code>ps</code>.</p><p>Next, quit Firefox, then run it again from the command line:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">firefox</span></span></code></pre></div><p>This command starts Firefox as a <strong>foreground</strong> process. A foreground process remains in control of the terminal. You cannot execute any commands until the process finishes.</p><p>Quit Firefox to regain control of the terminal, then run the following command:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">firefox</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;</span></span></code></pre></div><p>The ampersand (<strong><code>&amp;</code></strong>) at the end of this command starts Firefox as a <strong>background</strong> process. Bash will print its PID, for example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[1] 3776</span></span></code></pre></div><p>Background processes may continue to print messages to the terminal, but they don’t block you from running other commands. For example, while Firefox is running, press Enter to start a new prompt, and rerun this command from earlier:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ps</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -e</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> firefox</span></span></code></pre></div><p>Finally, quit Firefox, then press Enter in the terminal. Bash will confirm the process has finished:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[1]+  Done                       firefox</span></span></code></pre></div><div class="info custom-block"><p class="custom-block-title">Note</p><p>A single command may create multiple processes, such as when using pipes (<code>|</code>). For this reason, Bash uses <strong>job numbers</strong> (not PIDs) to track background jobs. In the example above, <code>[1]</code> was the job number.</p></div><h3 id="daemons" tabindex="-1">Daemons <a class="header-anchor" href="#daemons" aria-label="Permalink to &quot;Daemons&quot;">​</a></h3><p>Earlier in this lab, you used <code>ps</code> to list every process on the system:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ps</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -e</span></span></code></pre></div><p>Most of these processes aren’t associated with a terminal — as indicated by the <code>?</code> in the TTY column — and don’t require user interaction. These processes are known as <strong>daemons</strong>.</p><p>Daemons provide important system services. They are usually created during system startup and continue running until you stop them, or the system shuts down.</p><p>Daemons often have the suffix “d” in their name, such as <code>systemd</code> or <code>sshd</code>, but this is just a convention, not a requirement.</p><h2 id="process-information" tabindex="-1">Process information <a class="header-anchor" href="#process-information" aria-label="Permalink to &quot;Process information&quot;">​</a></h2><p>Start Firefox, then use the <strong><code>pidof</code></strong> command to look up its PID:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pidof</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> firefox</span></span></code></pre></div><p>This command may list multiple PIDs, indicating either that you’ve launched multiple instances of Firefox, or that Firefox itself spawned multiple processes. In the first case, look for the largest PID (the most recently launched instance); in the second case, the smallest (the process that was created first).</p><p>If you don’t know the exact name of a process, use <strong><code>pgrep</code></strong> to perform a search. For example:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pgrep</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -u</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $USER </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">-f</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> snap</span></span></code></pre></div><p>This command searches for processes that belong to the current user (<code>-u $USER</code>) and have “snap” somewhere in their full command line (<code>-f</code>).</p><p>Once you have the PIDs of the processes you’re interested in, use <code>ps</code> and <code>top</code> to monitor them:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ps</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pidof</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> firefox</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">top</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -p</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pidof</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d,</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> firefox</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p><code>ps</code> supports multiple PIDs as arguments. However, <code>top</code> requires that you provide them as a single comma-separated argument for the <code>-p</code> option. Fortunately, <code>pidof</code> can provide such an argument by adding the <code>-d</code> option and specifying a delimiter (<code>,</code>).</p><h2 id="process-hierarchy" tabindex="-1">Process hierarchy <a class="header-anchor" href="#process-hierarchy" aria-label="Permalink to &quot;Process hierarchy&quot;">​</a></h2><p>Linux organizes processes using parent-child relationships. Every process has a unique PID and stores the PID of the process that created it as the <strong>Parent Process ID (PPID)</strong>. The resulting hierarchy is known as the <strong>process tree</strong>.</p><p>The <strong><code>pstree</code></strong> command visualizes this process tree:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pstree</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -T</span></span></code></pre></div><p>The <code>-T</code> option hides <strong>threads</strong>. Threads allow a process to perform multiple tasks in parallel, but they are out-of-scope for this course and clutter the output of <code>pstree</code>.</p><p>Add the <code>-p</code> option to show PIDs:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pstree</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -pT</span></span></code></pre></div><p>To view the ancestry of a single process, add the <code>-s</code> option and specify a PID. For example, leave the <code>top</code> command running in your terminal, then open an additional tab and run the following command to see the ancestry of <code>top</code>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pstree</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -pT</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -s</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pidof</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> top</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>In my case, the output of this command was:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>systemd(1)</span></span>
<span class="line"><span>└─ systemd(1482)</span></span>
<span class="line"><span>   └─ ptyxis(2981)</span></span>
<span class="line"><span>      └─ ptyxis-agent(2989)</span></span>
<span class="line"><span>         └─ bash(3013)</span></span>
<span class="line"><span>            └─ top(4300)</span></span></code></pre></div><div class="info custom-block"><p class="custom-block-title">Note</p><p>The formatting of this output was adjusted for readability.</p></div><p>This output shows that <code>top</code> was created by <code>bash</code>, the shell, which was created by <code>ptyxis</code>, the terminal application, which was created by <code>systemd</code>.</p><p><strong><code>systemd</code></strong> is the first process created by the kernel and manages system services. As the root of the process tree, it receives PID 1.</p><p><code>systemd</code> plays an important role during system startup. Here’s what happens when you power on your computer:</p><ol><li>The CPU starts executing instructions from a predefined memory location. These instructions, known as <strong>firmware</strong>, are stored on a small read-only memory chip on the computer’s motherboard. Modern systems use the <strong>Unified Extensible Firmware Interface (UEFI)</strong>; older systems use the <strong>Basic Input/Output System (BIOS)</strong>.</li><li>The firmware initializes essential hardware and performs a <strong>Power-On Self-Test (POST)</strong> to verify that the hardware is functioning correctly.</li><li>The firmware searches for a bootable device, loads a bootloader from that device into memory, and transfers control to it. Most Linux systems use the <strong>Grand Unified Bootloader (GRUB)</strong>.</li><li>The bootloader searches the <strong>/boot</strong> directory for a compressed kernel image and unpacks it into memory. It then loads a temporary root file system: the <strong>Initial RAM File System (initramfs)</strong>. This file system contains the minimal tools and drivers needed to mount the actual root file system, after which it is discarded.</li><li>The bootloader transfers control to the kernel, which initializes the hardware and sets up the necessary data structures for managing processes, memory, and devices.</li><li>The kernel starts the <strong>init</strong> system, which starts and manages all other system services. Most Linux systems use <code>systemd</code> as the init system.</li><li>Finally, the init system starts a desktop environment or terminal, allowing a user to sign in and start using the system.</li></ol><h2 id="signals" tabindex="-1">Signals <a class="header-anchor" href="#signals" aria-label="Permalink to &quot;Signals&quot;">​</a></h2><p>Processes communicate with the operating system and with each other through <strong>signals</strong>. A signal is a software interrupt — a notification sent to a process to inform it that a specific event has occurred. Processes can register a <strong>handler</strong> to respond to a signal, or perform a default action. As a user, you’ll mostly use signals to stop a running process.</p><p>The <strong>SIGINT</strong> (<em>keyboard interrupt</em>) signal is sent to the foreground process when the user presses <strong>Ctrl+C</strong> in the terminal. The default action for this signal is to terminate the process, but the process can choose to handle it, cancel its current task, and continue executing.</p><p>In <a href="./working-with-text.html">Working with Text</a>, you used <code>tail</code> to watch a log file for changes:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">tail</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> log.txt</span></span></code></pre></div><p>This process runs indefinitely, so you need a signal to stop it. Press <strong>Ctrl+C</strong> to send SIGINT.</p><p>The <strong>SIGTERM</strong> (<em>graceful termination</em>) signal asks a process to terminate. This signal is stronger than SIGINT, which only asks a process to cancel its current task. The default action for SIGTERM is to terminate the process, but the process should handle it and perform any necessary cleanup first.</p><p>Use the <strong><code>kill</code></strong> command to send a SIGTERM signal. For example, run the previous <code>tail</code> command again, then open an additional tab and run the following command:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">kill</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pidof</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tail</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>The <code>kill</code> command can send any signal by specifying its name as an argument. For example, here’s how you send SIGINT instead of the default SIGTERM:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">kill</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -INT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pidof</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tail</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>Any well-behaved process should handle SIGTERM and perform a graceful shutdown. However, a process may become stuck and unable to respond to signals. In that case, you can send the <strong>SIGKILL</strong> (<em>forced termination</em>) signal. This signal is handled by the kernel and forces the process to exit.</p><p>Here’s how you send SIGKILL instead of SIGTERM:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">kill</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -KILL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pidof</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tail</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>The targeted process will not be able to handle this signal or perform any cleanup. Therefore, you should only use SIGKILL as a last resort and prefer SIGINT or SIGTERM whenever possible.</p><h2 id="up-next" tabindex="-1">Up next <a class="header-anchor" href="#up-next" aria-label="Permalink to &quot;Up next&quot;">​</a></h2><p>In this lab, you learned about processes and how to manage them. Practice these skills by solving the upcoming exercises.</p><p>When you’re done, proceed to the next lab, where you&#39;ll learn about file systems.</p>`,107)])])}const g=s(o,[["render",n]]);export{u as __pageData,g as default};

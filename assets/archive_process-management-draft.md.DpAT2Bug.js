import{_ as s,o as t,c as i,a0 as a}from"./chunks/framework.1R7PBCUH.js";const g=JSON.parse('{"title":"Process Management","description":"","frontmatter":{},"headers":[],"relativePath":"archive/process-management-draft.md","filePath":"archive/process-management-draft.md"}'),n={name:"archive/process-management-draft.md"};function o(r,e,h,d,l,p){return t(),i("div",null,[...e[0]||(e[0]=[a(`<h1 id="process-management" tabindex="-1">Process Management <a class="header-anchor" href="#process-management" aria-label="Permalink to &quot;Process Management&quot;">​</a></h1><h2 id="learning-objectives" tabindex="-1">Learning objectives <a class="header-anchor" href="#learning-objectives" aria-label="Permalink to &quot;Learning objectives&quot;">​</a></h2><p>In this lab assignment you will learn:</p><ul><li>Query and interpret an overview of active processes</li><li>View the hierarchical structure of processes</li><li>Start processes in the foreground and background</li><li>Look up a running process by name or PID</li><li>Stop a process using a kill signal</li></ul><h2 id="introduction" tabindex="-1">Introduction <a class="header-anchor" href="#introduction" aria-label="Permalink to &quot;Introduction&quot;">​</a></h2><p>Processes are the fundamental units of execution in an operating system. They represent running instances of programs and are responsible for executing code, managing resources, and performing tasks on behalf of users and applications. Understanding how processes work and how to manage them is crucial for system administration, performance optimization, and troubleshooting.</p><h2 id="what-is-a-process" tabindex="-1">What is a process? <a class="header-anchor" href="#what-is-a-process" aria-label="Permalink to &quot;What is a process?&quot;">​</a></h2><p>(TODO: stuk over compileren? compileren staat ook bij OS)</p><p>A process is an instance of a running program. When you execute a command in the terminal, the operating system creates a new process to run that command. When you execute the same command multiple times, each execution creates a separate process sharing the same program code but with its own memory space and execution context.</p><h3 id="address-space" tabindex="-1">Address space <a class="header-anchor" href="#address-space" aria-label="Permalink to &quot;Address space&quot;">​</a></h3><p>When creating a process, the operating system reserves a block of memory for it, which includes the program code, data, and stack. This memory is called the &quot;address space&quot;. The address space typically contains the following segments:</p><ul><li>Stack: temporary storage for variables, function parameters, return addresses from functions, etc. If there is no more space on the stack, due to for example too deep or infinite recursion, one gets the infamous stack overflow error.</li><li>Heap: dynamically allocated memory. If something is too large to place on the stack, or if something needs to be available across function boundaries, the stack is not a viable option. In that case, the heap can be used to dynamically allocate something. The heap does not work like a stack: what is stored there remains available until it is cleaned up by the programmer.</li><li>Data: global variables.</li><li>Text: the instructions to be executed.</li></ul><p>Keep in mind that the address space may differ between operating systems and architectures, but the general concept of separate memory segments for code, data, and stack is common across most systems.</p><p>The process also has a state (running, sleeping, stopped, etc.) and may have child processes that it has spawned.</p><h2 id="querying-active-processes" tabindex="-1">Querying active processes <a class="header-anchor" href="#querying-active-processes" aria-label="Permalink to &quot;Querying active processes&quot;">​</a></h2><p>(intro tot het bekijken van actieve processen via ps, top, htop, btop)</p><p>In Linux, you can use various commands to view active processes and their details. The most basic command is <code>ps</code>, which provides a snapshot of the current processes. The <code>-e</code> flag shows all processes, and the <code>-f</code> flag provides a full-format listing with additional details (e.g. the command that started the process, the user who owns it, etc.).</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ps</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -ef</span></span></code></pre></div><p>The output of <code>ps -ef</code> contains several columns:</p><table tabindex="0"><thead><tr><th>Column</th><th>Description</th></tr></thead><tbody><tr><td>UID</td><td>The user ID of the process owner</td></tr><tr><td>PID</td><td>The unique process identifier</td></tr><tr><td>PPID</td><td>The parent process ID — the PID of the process that created this one</td></tr><tr><td>C</td><td>CPU utilization (percentage)</td></tr><tr><td>STIME</td><td>The time at which the process started</td></tr><tr><td>TTY</td><td>The terminal associated with the process (<code>?</code> means no terminal, i.e. a background system process)</td></tr><tr><td>TIME</td><td>Total CPU time consumed by the process</td></tr><tr><td>CMD</td><td>The command that started the process</td></tr></tbody></table><p>Another useful command is <code>top</code>, which provides a real-time, interactive view of the system&#39;s processes. It shows CPU and memory usage, as well as other details about each process. You can sort processes by different criteria (e.g. CPU usage, memory usage) and interact with them (e.g. kill a process directly from the interface).</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">top</span></span></code></pre></div><p>Try typing <code>?</code> while in <code>top</code> to see the available commands and options. You&#39;ll quickly notice that the interface is not very user-friendly, which is where <code>htop</code> comes in. <code>htop</code> is an improved version of <code>top</code> with a more intuitive interface, color coding, and additional features.</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> apt</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -y</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> htop</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">htop</span></span></code></pre></div><p>As you can see in <code>htop</code>, each process has a PID. The PID is a unique identifier assigned to each process by the operating system. It is used to manage and track processes, allowing you to perform actions such as sending signals (e.g. to stop a process) or changing its priority.</p><p>You&#39;ll also see a column &quot;TIME+&quot; in <code>htop</code>, which shows the total CPU time consumed by the process since it started. You might notice that some process have a value of 0:00.00 in this column, which means that they have not consumed any CPU time yet. This can happen if the process is sleeping or waiting for an event, or if it has just started and has not had a chance to execute any instructions.</p><h2 id="process-states" tabindex="-1">Process states <a class="header-anchor" href="#process-states" aria-label="Permalink to &quot;Process states&quot;">​</a></h2><p>At any given moment, a process is in one of several states. You can see the state of each process in the <code>S</code> column in <code>htop</code>.</p><table tabindex="0"><thead><tr><th>State</th><th>Code</th><th>Description</th></tr></thead><tbody><tr><td>Running</td><td><code>R</code></td><td>The process is currently being executed by the CPU, or is in the run queue waiting to be executed.</td></tr><tr><td>Sleeping</td><td><code>S</code></td><td>The process is waiting for an event (e.g. user input, a timer, or data from disk). It will become runnable again once the event occurs.</td></tr><tr><td>Uninterruptible sleep</td><td><code>D</code></td><td>Similar to sleeping, but the process cannot be interrupted by signals. This usually happens when the process is waiting for I/O (e.g. reading from disk).</td></tr><tr><td>Stopped</td><td><code>T</code></td><td>The process has been paused, typically by a signal such as <code>SIGSTOP</code>. It will not execute until it receives a <code>SIGCONT</code> signal.</td></tr><tr><td>Zombie</td><td><code>Z</code></td><td>The process has finished executing, but its entry is still in the process table because its parent has not yet read its exit status. Zombies consume no resources and disappear once the parent reads the exit code.</td></tr></tbody></table><p>(TODO: btop in exercise?)</p><h2 id="scheduling" tabindex="-1">Scheduling <a class="header-anchor" href="#scheduling" aria-label="Permalink to &quot;Scheduling&quot;">​</a></h2><p>The operating systems scheduler is responsible for allocating CPU time to processes. It uses various algorithms to determine which process should run next based on factors such as priority, CPU usage, and waiting time. When a process is scheduled to run, it is given access to the CPU and can execute its instructions. If a process is sleeping or waiting for an event, it will not consume CPU time until it becomes active again.</p><p>In the meantime the process state is kept in memory. When the process becomes active again, all necessary information is retrieved from memory, moved over to the CPU and the process can continue executing from where it left off. This is how the operating system manages multiple processes and allows them to share the CPU effectively.</p><h3 id="scheduling-algorithms" tabindex="-1">Scheduling algorithms <a class="header-anchor" href="#scheduling-algorithms" aria-label="Permalink to &quot;Scheduling algorithms&quot;">​</a></h3><p>(TODO: schematische voorstelling van scheduling?)</p><p>Scheduling algorithms differ primarily in one dimension: whether they allow the scheduler to interrupt a running process.</p><h4 id="non-preemptive-scheduling" tabindex="-1">Non-preemptive scheduling <a class="header-anchor" href="#non-preemptive-scheduling" aria-label="Permalink to &quot;Non-preemptive scheduling&quot;">​</a></h4><p>In non-preemptive scheduling, once a process is given the CPU it runs until it either finishes or voluntarily yields. This simplifies the scheduler but hurts responsiveness — a single long-running process can block everything else.</p><p>Common non-preemptive algorithms:</p><ul><li><strong>First-Come, First-Served (FCFS)</strong>: Processes are run in arrival order. Simple, but can cause long wait times when a slow process lands at the front of the queue.</li><li><strong>Shortest Process Next (SPN)</strong>: The process with the shortest expected execution time runs next. Minimizes average wait time, but longer processes can be starved indefinitely if short ones keep arriving.</li></ul><h4 id="preemptive-scheduling" tabindex="-1">Preemptive scheduling <a class="header-anchor" href="#preemptive-scheduling" aria-label="Permalink to &quot;Preemptive scheduling&quot;">​</a></h4><p>In preemptive scheduling, the scheduler can pause a running process and switch to another one. This keeps the system responsive and prevents any single process from monopolizing the CPU.</p><p>Common preemptive algorithms:</p><ul><li><strong>Shortest Remaining Time (SRT)</strong>: The preemptive variant of SPN. If a newly arrived process has a shorter remaining time than the current one, the scheduler immediately switches to it.</li><li><strong>Round Robin (RR)</strong>: Each process gets a fixed time slice (e.g. 100 ms). If it doesn&#39;t finish in time, it is preempted and moved to the back of the ready queue, giving every process a fair turn.</li></ul><h2 id="booting-the-system" tabindex="-1">Booting the system <a class="header-anchor" href="#booting-the-system" aria-label="Permalink to &quot;Booting the system&quot;">​</a></h2><p>When the computer is powered on, it goes through a boot process that initializes the hardware and starts the operating system. The boot process typically involves several stages:</p><ol><li><strong>BIOS/UEFI</strong>: The Basic Input/Output System (BIOS) or Unified Extensible Firmware Interface (UEFI) is the first code that runs when the computer is powered on. It performs hardware initialization and checks for a bootable device. It searches for a bootloader on the storage devices (e.g. hard drive, SSD) and loads it into memory.</li><li><strong>Bootloader</strong>: The bootloader (e.g. GRUB in the case of Linux) is responsible for loading the operating system kernel into memory and transferring control to it.</li><li><strong>Unpacking the kernel and initramfs</strong>: The kernel and initramfs are compressed to save space on disk and are unpacked into memory during the boot process. The initramfs (initial RAM filesystem) is a temporary root filesystem loaded into memory before the real root filesystem is available. It contains the minimal tools and drivers needed to mount the actual root filesystem, after which it is discarded.</li><li><strong>Kernel initialization</strong>: The bootloader transfers control to the kernel, which initializes the hardware and sets up the necessary data structures for managing processes, memory, and devices.</li><li><strong>Starting the init system</strong>: The kernel starts the init system (e.g. systemd), which is responsible for starting and managing all other processes on the system. The init system reads its configuration files to determine which services and processes to start at boot time. The init system is the first process started by the kernel and is assigned PID 1. Every other process on the system is a descendant of this process, making it the root of the entire process tree.</li></ol><p>On Linux, the files involved in the boot process can be found in the <code>/boot</code> directory. This directory contains the kernel, initramfs, and the bootloader configuration files, among others.</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ls</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -l</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /boot</span></span></code></pre></div><p>In <code>/boot</code> you&#39;ll typically find the following files:</p><ul><li><code>vmlinuz-&lt;version&gt;</code>: The compressed Linux kernel image.</li><li><code>initrd.img-&lt;version&gt;</code>: The initramfs image.</li><li><code>grub/</code>: A directory containing the GRUB bootloader configuration files and modules.</li></ul><h2 id="process-hierarchy" tabindex="-1">Process hierarchy <a class="header-anchor" href="#process-hierarchy" aria-label="Permalink to &quot;Process hierarchy&quot;">​</a></h2><p><code>systemd</code> is the init system used by many Linux distributions. It is responsible for starting and managing all other processes on the system. Since <code>systemd</code> is the first process started by the kernel, it has PID 1 and is the root of the entire process tree. All other processes on the system are descendants of <code>systemd</code>, either directly or indirectly.</p><p>The process hierarchy can be visualized as a tree structure, where <code>systemd</code> is the root and all other processes are branches stemming from it.</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pstree</span></span></code></pre></div><p>(TODO: schematische voorstelling van een process tree via mermaid?)</p><p>When you pass the <code>-p</code> flag to <code>pstree</code>, it will show the PID of each process in parentheses next to its name. This can help you understand the parent-child relationships between processes and identify which processes are running under which parents. It will enlarge the output as <code>pstree</code> will by default merge identical processes into a single line with a count.</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pstree</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -p</span></span></code></pre></div><h2 id="starting-processes" tabindex="-1">Starting processes <a class="header-anchor" href="#starting-processes" aria-label="Permalink to &quot;Starting processes&quot;">​</a></h2><p>When you run a command in the terminal, the shell starts a new child process to execute it. For example, running <code>firefox</code> launches the Firefox browser as a child of the shell process.</p><p>The new process inherits certain properties from its parent, such as environment variables and open file descriptors, and can receive additional arguments at start-up. The operating system also allocates dedicated resources for it, such as memory and CPU time.</p><p>Every process is assigned a unique <strong>Process ID (PID)</strong> by the kernel, along with a <strong>Parent Process ID (PPID)</strong> that links it back to the process that created it.</p><h3 id="foreground-vs-background-processes" tabindex="-1">Foreground vs background processes <a class="header-anchor" href="#foreground-vs-background-processes" aria-label="Permalink to &quot;Foreground vs background processes&quot;">​</a></h3><p>A process can be started in the foreground or in the background. When a process is started in the foreground, it takes control of the terminal and the user cannot execute any other commands until the process finishes.</p><p>When a process is started in the foreground, it blocks the terminal until it finishes. Try running <code>sleep</code> for 10 seconds:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sleep</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 10</span></span></code></pre></div><p>Notice that the terminal is unresponsive for the duration. You can cancel the process early by pressing <code>Ctrl+C</code>, which sends a signal to terminate it.</p><p>When a process is started in the background, it runs independently of the terminal and you can continue executing other commands while it is running. In bash, you start a process in the background by appending an ampersand (<code>&amp;</code>) to the command:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sleep</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 10</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;</span></span></code></pre></div><p>You will immediately see output like <code>[1] 3428</code>, where <code>[1]</code> is the job number assigned by the shell and <code>3428</code> is the PID of the new process. The terminal remains available and you can keep working. Once the background process finishes, the shell will print a notification like <code>[1]+ Done sleep 10</code> the next time you press Enter.</p><h2 id="looking-up-processes" tabindex="-1">Looking up processes <a class="header-anchor" href="#looking-up-processes" aria-label="Permalink to &quot;Looking up processes&quot;">​</a></h2><p>Once a process is running, you often need to find its PID — for example to inspect it, change its priority, or stop it. Start Firefox in the background so it keeps running while you work:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">firefox</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;</span></span></code></pre></div><p>You&#39;ll notice that however Firefox is running in the background, it will show the process&#39; output in your terminal. This is because the standard output and error of the process are still connected to the terminal.</p><p>You can now look up its PID by name using <code>pidof</code> (in a separate terminal):</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pidof</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> firefox</span></span></code></pre></div><p><code>pidof</code> returns the PID (or multiple PIDs if several instances are running) of a process by its name. You can then use that PID with <code>ps</code> to get more details about the process:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ps</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -p</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pidof</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> firefox</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>TODO: hebben ze $(...) al gezien?</p><p>This shows the same columns as <code>ps -ef</code>, but filtered to just the process you are interested in. The <code>$(...)</code> syntax runs <code>pidof firefox</code> first and passes its output as an argument to <code>ps</code>.</p><p>If you want to search more broadly — for example when you do not know the exact process name — you can use <code>pgrep</code>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pgrep</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -a</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> fire</span></span></code></pre></div><p>The <code>-a</code> flag also prints the full command line, not just the PID. <code>pgrep</code> matches against the process name as a substring, so <code>fire</code> is enough to find Firefox.</p><h2 id="inspecting-proc" tabindex="-1">Inspecting /proc <a class="header-anchor" href="#inspecting-proc" aria-label="Permalink to &quot;Inspecting /proc&quot;">​</a></h2><p>Every operating system keeps track of all processes running on the system in a process table. Linux exposes this information through a special virtual filesystem called <code>/proc</code>. This filesystem contains a directory for each active process, named after its PID. Inside each process directory, there are various files that provide information about the process, such as its status, memory usage, open file descriptors, and more.</p><p>Check out the contents of the process directory for one of the Firefox processes you found earlier:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ls</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /proc/</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">pi</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span></code></pre></div><p>Each entry in that directory represents a property or resource of the process. The table below covers the most important ones:</p><table tabindex="0"><thead><tr><th>Entry</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>cmdline</code></td><td>file</td><td>The full command used to start the process, with arguments separated by null bytes.</td></tr><tr><td><code>status</code></td><td>file</td><td>Human-readable summary: name, PID, PPID, current state, and memory usage figures.</td></tr><tr><td><code>stat</code></td><td>file</td><td>Machine-readable version of the same data, used internally by tools such as <code>ps</code> and <code>top</code>.</td></tr><tr><td><code>environ</code></td><td>file</td><td>The environment variables the process was started with, separated by null bytes.</td></tr><tr><td><code>exe</code></td><td>symlink</td><td>Points to the executable binary that was launched.</td></tr><tr><td><code>cwd</code></td><td>symlink</td><td>Points to the process&#39;s current working directory.</td></tr><tr><td><code>fd/</code></td><td>directory</td><td>Contains one symlink per open file descriptor (stdin, stdout, stderr, open files, sockets, …).</td></tr><tr><td><code>maps</code></td><td>file</td><td>The virtual memory map: which address ranges are in use and what is mapped there (code, heap, stack, shared libs).</td></tr><tr><td><code>io</code></td><td>file</td><td>I/O statistics: how many bytes the process has read from and written to storage since it started.</td></tr><tr><td><code>limits</code></td><td>file</td><td>The resource limits in effect for the process (e.g. maximum number of open files, maximum stack size).</td></tr><tr><td><code>task/</code></td><td>directory</td><td>Contains one subdirectory per thread. Each subdirectory has the same layout as the process directory itself.</td></tr></tbody></table><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cat</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /proc/</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">pi</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/status</span></span></code></pre></div><p>This shows the name, PID, PPID, current state, and memory usage of the process in a readable format. Which of these fields do you recognize from the output of <code>ps</code>?</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cat</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /proc/</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">pi</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/cmdline</span></span></code></pre></div><p>This prints the exact command used to launch the process. Because arguments are separated by null bytes rather than spaces, pipe through <code>tr</code> to make it readable:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cat</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /proc/</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">pi</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/cmdline</span></span></code></pre></div><h2 id="stopping-processes" tabindex="-1">Stopping processes <a class="header-anchor" href="#stopping-processes" aria-label="Permalink to &quot;Stopping processes&quot;">​</a></h2><p>In Linux, processes communicate with each other and with the operating system through signals. A signal is a software interrupt — a notification sent to a process to inform it that a specific event has occurred. When a process receives a signal, it can respond in one of three ways:</p><ol><li><strong>Default action</strong>: Perform the built-in action for that signal (e.g. terminate, stop, or ignore).</li><li><strong>Custom handler</strong>: Execute a function the process registered for that signal, allowing it to clean up before exiting.</li><li><strong>Ignore</strong>: Discard the signal entirely.</li></ol><p>Signals are identified by both a name (e.g. <code>SIGTERM</code>) and a number (e.g. <code>15</code>). The most important ones are listed below:</p><table tabindex="0"><thead><tr><th>Signal</th><th>Number</th><th>Default action</th><th>Description</th></tr></thead><tbody><tr><td><code>SIGHUP</code></td><td>1</td><td>Terminate</td><td>Sent when the controlling terminal is closed. Daemons often use it as a trigger to reload their configuration.</td></tr><tr><td><code>SIGINT</code></td><td>2</td><td>Terminate</td><td>Sent when the user presses <code>Ctrl+C</code>. Asks the process to stop, but the process can catch and handle it.</td></tr><tr><td><code>SIGTERM</code></td><td>15</td><td>Terminate</td><td>The standard way to ask a process to stop. The process can catch the signal, perform cleanup, and then exit gracefully.</td></tr><tr><td><code>SIGKILL</code></td><td>9</td><td>Terminate</td><td>Forces the kernel to destroy the process immediately. Cannot be caught, ignored, or blocked — there is no opportunity to clean up.</td></tr><tr><td><code>SIGSTOP</code></td><td>19</td><td>Stop</td><td>Pauses the process. Like <code>SIGKILL</code>, it cannot be caught or ignored.</td></tr><tr><td><code>SIGCONT</code></td><td>18</td><td>Continue</td><td>Resumes a process that was previously stopped with <code>SIGSTOP</code>.</td></tr></tbody></table><p>The <code>kill</code> command sends a signal to a process by its PID. Despite its name, it is not limited to terminating processes — it can send any signal. Without a signal specified, it sends <code>SIGTERM</code> (15) by default:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">kill</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">pi</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">         # sends SIGTERM</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">kill</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -9</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">pi</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      # sends SIGKILL</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">kill</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -SIGSTOP</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">pi</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  # sends SIGSTOP — pauses the process</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">kill</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -SIGCONT</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">pi</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  # sends SIGCONT — resumes it</span></span></code></pre></div><p>Try to stop the Firefox process you started earlier using <code>kill</code>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">kill</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pidof</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> firefox</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><h3 id="graceful-shutdown-with-sigterm" tabindex="-1">Graceful shutdown with SIGTERM <a class="header-anchor" href="#graceful-shutdown-with-sigterm" aria-label="Permalink to &quot;Graceful shutdown with SIGTERM&quot;">​</a></h3><p>Start the simulation script in the background:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">chmod</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> u+x</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> kill-signals.sh</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">./kill-signals.sh</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;</span></span></code></pre></div><p>Note the PID printed by the shell, then send <code>SIGTERM</code> to it:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">kill</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">pi</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span></code></pre></div><p><code>kill-signals.sh</code> catches <code>SIGTERM</code>, prints a message, and exits cleanly. You can observe the output in the terminal — the process has a chance to finish any work in progress before it stops.</p><h3 id="forced-termination-with-sigkill" tabindex="-1">Forced termination with SIGKILL <a class="header-anchor" href="#forced-termination-with-sigkill" aria-label="Permalink to &quot;Forced termination with SIGKILL&quot;">​</a></h3><p>Start the script again and this time send <code>SIGKILL</code>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">./kill-signals.sh</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">kill</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -9</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">pi</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span></code></pre></div><p>The process is destroyed by the kernel immediately, with no opportunity to run a cleanup handler. You will not see the graceful-shutdown message this time.</p><details><summary> When should we use <code>kill &lt;pid&gt;</code> versus <code>kill -9 &lt;pid&gt;</code>? </summary><p><code>kill &lt;pid&gt;</code> sends <strong>SIGTERM</strong> (15): the process receives the signal and can handle it — running cleanup code, flushing buffers, closing connections — before it exits. The process can also choose to ignore it entirely.</p><p><code>kill -9 &lt;pid&gt;</code> sends <strong>SIGKILL</strong> (9): the kernel destroys the process immediately, bypassing any signal handler. There is no cleanup, no graceful shutdown, no way for the process to refuse.</p><p>Always try <code>kill &lt;pid&gt;</code> first. Well-written processes handle SIGTERM correctly and exit cleanly. Fall back to <code>kill -9 &lt;pid&gt;</code> only if the process does not respond — for example if it is frozen or stuck in an infinite loop ignoring signals. Be aware that a SIGKILL&#39;d process may leave behind partial writes, open sockets, or lock files that need manual cleanup.</p></details>`,114)])])}const u=s(n,[["render",o]]);export{g as __pageData,u as default};

import{_ as t,o,c as s,a0 as r}from"./chunks/framework.1R7PBCUH.js";const u=JSON.parse('{"title":"Exercises 5","description":"","frontmatter":{},"headers":[],"relativePath":"exercises/exercises5.md","filePath":"exercises/exercises5.md"}'),a={name:"exercises/exercises5.md"};function i(n,e,c,d,h,l){return o(),s("div",null,[...e[0]||(e[0]=[r('<h1 id="exercises-5" tabindex="-1">Exercises 5 <a class="header-anchor" href="#exercises-5" aria-label="Permalink to &quot;Exercises 5&quot;">​</a></h1><p>The following exercises use files from the <strong>scripts</strong> directory of the lab materials. Navigate to that directory first.</p><h2 id="memory" tabindex="-1">Exercise 5.1 <a class="header-anchor" href="#memory" aria-label="Permalink to &quot;Exercise 5.1 {#memory}&quot;">​</a></h2><p>Run the script <strong>memory-usage.py</strong>. In a different tab, look up the PID of this script and find out how much memory it’s consuming. What do you observe?</p><h2 id="cpu" tabindex="-1">Exercise 5.2 <a class="header-anchor" href="#cpu" aria-label="Permalink to &quot;Exercise 5.2 {#cpu}&quot;">​</a></h2><p>Run the script <strong>cpu-usage.py</strong>. In a different tab, look up the PID of this script and find out how much CPU time it’s consuming. What do you observe?</p><h2 id="htop" tabindex="-1">Exercise 5.3 <a class="header-anchor" href="#htop" aria-label="Permalink to &quot;Exercise 5.3 {#htop}&quot;">​</a></h2><p>In this exercise, you’ll install and use <strong><code>htop</code></strong>, an alternative to <code>top</code> which provides a more intuitive interface.</p><p>First, use <code>apt</code> to install <code>htop</code>. Next, run <code>htop</code> and press <code>F2</code> to enter its setup screen. Use the arrow keys to navigate to “Display options”, find the “Hide userland process threads” setting, and press <code>Enter</code> to enable it. When you’re done, press <code>q</code> to return to the previous screen.</p><p>Now that <code>htop</code> is installed and configured, repeat <a href="#memory">Exercise 5.1</a> and <a href="#cpu">Exercise 5.2</a> using <code>htop</code> instead of <code>top</code>. Explore the following features:</p><ul><li>Press <code>F3</code> or <code>/</code> to perform a search. <code>htop</code> will highlight the first process that matches your query. Use <code>F3</code> and <code>Shift+F3</code> to jump between matches.</li><li>Press <code>F4</code> to filter the list of processes. <code>htop</code> will only show processes that match your query.</li><li>Press <code>F6</code> to choose a sort order.</li></ul><p>When you’re done, press <code>q</code> to quit.</p><p>Finally, <code>htop</code> also supports the <code>-p</code> option. Use this option to only see the processes you’re interested in, without having to search or filter first.</p><h2 id="exercise-5-4" tabindex="-1">Exercise 5.4 <a class="header-anchor" href="#exercise-5-4" aria-label="Permalink to &quot;Exercise 5.4&quot;">​</a></h2><p>In the previous lab, you used <code>pstree</code> to view the ancestry of a process. In this exercise, you’ll gather the same information using only <code>ps</code>.</p><p>Start <code>htop</code>, then open an additional tab and use <code>ps</code> to find its PID. Next, use the <code>-f</code> (<em>full-format</em>) option of <code>ps</code> to find the PPID of <code>htop</code>. Repeat this step for each parent process until you’ve worked your way up the process tree. Finally, compare your findings with the output of <code>pstree</code>.</p><div class="info custom-block"><p class="custom-block-title">Note</p><p><code>htop</code> can also show the process tree. Press <code>F5</code> to toggle between a list view and a tree view.</p></div><h2 id="signals" tabindex="-1">Exercise 5.5 <a class="header-anchor" href="#signals" aria-label="Permalink to &quot;Exercise 5.5 {#signals}&quot;">​</a></h2><p>Run the script <strong>well-behaved.py</strong>. Send it the signals SIGINT, SIGTERM, and SIGKILL, and observe how the script responds to these signals.</p><p>Next, perform the same steps for the script <strong>misbehaved.py</strong>. What do you observe?</p><h2 id="exercise-5-6" tabindex="-1">Exercise 5.6 <a class="header-anchor" href="#exercise-5-6" aria-label="Permalink to &quot;Exercise 5.6&quot;">​</a></h2><p>Signals can also be sent using their number instead of their name. Run the following command to list all available signals and their number:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">kill</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -l</span></span></code></pre></div><p>Consult the man page for <code>kill</code> to learn how you can send a signal using its number, then repeat <a href="#signals">Exercise 5.5</a> using signal numbers instead of names.</p><div class="info custom-block"><p class="custom-block-title">Note</p><p>Signal numbers can vary between systems, so you should always prefer names over numbers.</p></div><h2 id="exercise-5-7" tabindex="-1">Exercise 5.7 <a class="header-anchor" href="#exercise-5-7" aria-label="Permalink to &quot;Exercise 5.7&quot;">​</a></h2><p>In this exercise, you’ll explore the <strong>/proc</strong> directory, which contains a <a href="./../labs/files-and-directories.html#virtual-filesystem">virtual filesystem</a> that provides information about running processes.</p><p>The files in <strong>/proc</strong> aren’t stored on disk; the kernel provides them dynamically whenever you access them. Each running process has its own subdirectory in <strong>/proc</strong>, named after its PID.</p><p>To get started, run the script <strong>well-behaved.py</strong>, find its PID, and navigate to its subdirectory in <strong>/proc</strong>. Browse the contents of this directory with <code>ls</code> and answer the following questions:</p><ul><li>What is the current working directory of the process?</li><li>What executable is the process running?</li></ul><p>Next, read the contents of the <strong>cmdline</strong> file. This file contains the full command that started the process, including any options and arguments. Unfortunately, these options and arguments are separated by null bytes (<code>\\0</code>), which don’t display in your terminal. Use <code>tr</code> to replace these null bytes with spaces.</p><p>Next, read the contents of the <strong>environ</strong> file. This file contains the environment variables for the process, again separated by null bytes. Use <code>tr</code> to replace these null bytes with newline characters (<code>\\n</code>), then find the environment value that contains the current working directory.</p><p>Finally, read the contents of the <strong>status</strong> file. Find out how much memory the process is using and compare your findings to the numbers reported by <code>top</code> or <code>htop</code>.</p>',33)])])}const m=t(a,[["render",i]]);export{u as __pageData,m as default};
